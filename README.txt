Telepresence systems must have SSH enabled for the connection to establish. 
If SSH was disabled and you enable it, restart the system.

Interface Panel
Each Widget Id must be named after the CMS coSpace ID in order for the query on the API to work.